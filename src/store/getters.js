export const Customers = state => state.customers;

export const loggedIn = state => state.loggedIn;

export const singUpPage = state => state.signUpPage;

export const loginDetails = state => state.loginDetails;

export const isValidUser = state => state.isValidUser;

export const authorizedUser = state => state.authorizedUser;

export const userData = state=> state.userData;

export const message = state=> state.message;

export const errorMessage = state=> state.errorMessage;

export const loginErrorExist = state=> state.loginErrorExist;

export const userToken = state=> state.userToken;

export const key = state=> state.key;
